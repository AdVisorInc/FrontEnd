import { Divider, Typography } from '@mui/material';

const Component = () => {
  return (
    <Divider>
      <Typography variant="h5">Divider Title</Typography>
    </Divider>
  );
};

export default Component;
