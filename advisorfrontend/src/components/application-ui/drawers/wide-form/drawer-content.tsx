import { Container } from '@mui/material';
import ProfileFormStacked from 'src/components/application-ui/form-layouts/profile-stacked/profile-form-stacked';

const Component = () => {
  return (
    <Container maxWidth="sm">
      <ProfileFormStacked />
    </Container>
  );
};

export default Component;
