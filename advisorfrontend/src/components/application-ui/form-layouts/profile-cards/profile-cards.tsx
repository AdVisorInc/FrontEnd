import { Box, Container } from '@mui/material';
import ProfileCardsForm from './profile-cards-form';

const Component = () => {
  return (
    <Box py={{ xs: 2, md: 3 }}>
      <ProfileCardsForm />
    </Box>
  );
};

export default Component;
